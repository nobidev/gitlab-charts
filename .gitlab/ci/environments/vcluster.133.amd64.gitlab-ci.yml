include:
  - component: "gitlab.com/gitlab-org/quality/pipeline-common/allure-report@9.6.3"
    inputs:
      job_name: "e2e-test-v133"
      job_stage: "qa-report"
      gitlab_auth_token_variable_name: "DANGER_GITLAB_API_TOKEN"
      aws_access_key_id_variable_name: "QA_ALLURE_AWS_ACCESS_KEY_ID"
      aws_secret_access_key_variable_name: "QA_ALLURE_AWS_SECRET_ACCESS_KEY"
      allure_results_glob: "gitlab-qa-run-*/**/allure-results"
    rules:
      - if: '$CI_SERVER_HOST != "gitlab.com"'
        when: never
      - if: '$CI_PIPELINE_SOURCE == "pipeline" || $CI_PIPELINE_SOURCE == "parent_pipeline"'

.v133_base:
  extends:
  - .vcluster_base
  variables:
    VCLUSTER_K8S_VERSION: "v1.33.0"
    HOST_SUFFIX: "${CI_ENVIRONMENT_SLUG}-vc133"
    # the VARIABLES_FILE value is from the CI_JOB_NAME of the review job
    VARIABLES_FILE: "variables/review_v133"
    DOMAIN: "-${HOST_SUFFIX}.${KUBE_INGRESS_BASE_DOMAIN}"
  environment:
    name: gkevc_133/$REVIEW_REF_PREFIX$CI_COMMIT_SHORT_SHA
    url: "https://gitlab${DOMAIN}"

create_review_v133:
  stage: review
  image: ${BUSYBOX_IMAGE}
  extends: .v133_base
  variables:
    GIT_STRATEGY: none
  script:
    - echo "Configuring gkevc_133/$REVIEW_REF_PREFIX$CI_COMMIT_SHORT_SHA to automatically stop in $REVIEW_APPS_AUTO_STOP_IN."
  environment:
    action: start
    on_stop: stop_review_v133
    auto_stop_in: $REVIEW_APPS_AUTO_STOP_IN

review_v133:
  extends:
    - .vcluster_review_template
    - .v133_base
  environment:
    action: access

stop_review_v133:
  extends:
    - .stop_vcluster_review_template
    - .v133_base
  environment:
    action: stop

review_specs_v133:
  extends:
    - .vcluster_specs_template
    - .v133_base
  environment:
    action: access
  needs: ['review_v133']

qa_v133:
  extends:
    - .qa_branch
    - .v133_base
  variables:
    TEST_SUITE: $QA_SANITY_SUITE_OPTIONS
  environment:
    action: access
  needs:
    - review_v133
    - job: review_specs_v133
      artifacts: false

qa_v133_full_suite_manual_trigger:
  extends: .qa_full_suite_manual_trigger
  script: echo 'The job will trigger Full E2E suite against GKE133 vcluster'

qa_v133_manual_full_suite:
  extends:
  - .qa_branch
  - .v133_base
  parallel: 7
  variables:
    TEST_SUITE: $QA_FULL_SUITE_OPTIONS
  environment:
    action: access
  needs: ["qa_v133_full_suite_manual_trigger"]
